<Window x:Class="VirtualFAT.NotBad"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VirtualFAT"
        mc:Ignorable="d"
        Icon="Images/fat32-formatter.png"
        Title="NotBad++" Height="450" Width="800">
    <Grid>
        <DockPanel HorizontalAlignment="Left" Height="408" LastChildFill="False" Margin="10,1,0,0" VerticalAlignment="Top" Width="772">
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_Save" Click="Save_Click"/>
                <MenuItem Header="_Close" Click="Close_Click"/>
            </Menu>
            <StackPanel DockPanel.Dock="Right" Width="770">
                <TextBox 
                    TextWrapping="Wrap" 
                    AcceptsReturn="True"
                    VerticalScrollBarVisibility="Visible" 
                    TextChanged="TextBox_TextChanged"
                    x:Name="TextContent"
                    Height="388"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
